$$
\text { 56 Algebra Ch. 8 }
$$

ring and Jacobson semisimple, by Corollary 8.3(5). The factor module $P^{(1)} / P^{(k+1)}$ is an $R/I$-module; hence, by Corollary 8.3(5), $P^{(1)} / P^{(k+1)}$ is a semisimple module, and so it can be decomposed into a direct sum of (possibly infinitely many) simple $R/I$-modules. But there can be only finitely many summands, for every $R/I$-submodule of $P^{(1)} / P^{(k+1)}$ is necessarily an $R$-submodule, and $P^{(1)} / P^{(k+1)}$ has the DCC on $R$-submodules. Hence, there are only finitely many $R/I$-modules $S_i$.

Throwing away one simple submodule at a time yields a series of $P^{(1)} / P^{(k+1)}$ whose $R$-factor modules are

$$
(S, S_1, \ldots, S_k, 0) \text{ or } (S, S_1, \ldots, S_{k-1}, 0) \text{ or } (S, S_1, \ldots, S_{k-2}, 0)
$$

Now the simple $R/I$-module $S_i$ is also a simple $R$-module, for it is an $R$-module annihilated by $I$, so that we have constructed a composition series for $P^{(1)} / P^{(k+1)}$ as an $R$-module. Finally, refine the original series for $P^{(1)} / P^{(k+1)}$ in this way, for every $i$, to obtain a composition series for $R \cdot \mathbf{a}$.

Of course, the converse of Theorem 8.6 is false.

The next result is fundamental.

**Theorem 8.47 (MacDuffie's Theorem).** If $G$ is a finite group and $k$ is a field where characteristic does not divide $|G|$, then $kG$ is a left semisimple ring.

**Remark.** The hypothesis always holds if $k$ has characteristic 0.

**Proof.** By Proposition 8.42, it suffices to prove that every left ideal $I$ of $kG$ is a direct sum of simple left $kG$-modules. Since $k$ is a field, $kG$ is a vector space over $k$, and so it has a basis. Let $I$ be a subspace of $kG$. By Corollary 8.43, if $I$ is a vector space of dimension $n$, then there is a basis $\{x_1, \ldots, x_n\}$ for $I$ such that for each $i$, $x_i$ is a left ideal of $kG$ with $x_i \cdot kG = x_i$. There is a linear transformation $D: kG \to kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a basis $\{x_1, \ldots, x_n\}$ for $kG$ such that for each $i$, $D(x_i) = x_i$. Since $kG$ is a finite-dimensional vector space over $k$, there is a
